import FlightModel from "../../models/FlightModel";

export const SeatPage: React.FC<{ flight: FlightModel }> = (props) => {

    // Query row and column for aircraft
    const seatColumns: number = 4;
    const seatRows: number = 5;

    // Query seats for flight id

    return (
        <div className={'container'}>
        </div>
    );
};