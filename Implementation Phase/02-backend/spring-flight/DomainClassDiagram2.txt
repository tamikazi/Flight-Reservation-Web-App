@startuml

class Airline {
- name: string
}

class Flight {
- flightID: number
- code: string
- origin: string
- destination: string
- departDate: Date
- departTime: Time

+ getByDate(date: Date): Flight[]
+ getByCodeAndDate(code: string, date: Date): Flight
+ getByDateAndOriginAndDestination(date: Date,
origin: string, destination: string): Flight[]
+ addFlight(all fields): void
+ updateFlight(all fields): void
+ deleteFlight(code: string, date: string): void
}

class Aircraft {
- aircraftID: number
- model: string
- numCols: number
- numRows: number

+ getAllAircraft(): Aircraft[]
+ addAircraft(all fields): void
+ deleteAircraft(aircraftID: number): void
+ getAircraft(aircraftID: number): Aircraft
}

class Crew {
- FName: string
- LName: string
- email: string
- password: string

+ getCrewList(id: number): Crew[]
}

class Ticket {
- flight: Flight
- seat: Seat
- user: User
- insurance: boolean

+ addTicket(all fields): void
+ deleteTicket(flightID: number, seatID: number): void
}

class Seat {
- seatID: number
- seatNumber: string
- class: string

+ getUnavailableSeats(flightID: number): Seat[]
}

class User {
- userID: number
- FName: string
- LName: string
- email: string
- password: string
- member: boolean
- role: string

+ getAllUsers(): User[]
+ login(username: string, password: string): boolean
+ getRole(username: string): string
+ logout(): void
+ getManifest(flightID: number): User[]
+ registerUser(): void
}

class Payment {
- paymentID: number
- userID: number
- date: Date
- amount: number

+ addPayment(all fields): void
+ getHistory(userID: number): Payment[]
}

class Email {
- addresses: string[]
- subject: string
- body: string

+ makeReceipt(payment: Payment, ticket: Ticket, user: User)
+ makeNewsletter(body: string, users: User[])
+ sendEmail(): void
}

Airline "1" -- "1..*" Flight : hosts
Airline "1" -- "1..*" Aircraft : owns
Flight "1..*" -- "1" Aircraft : flies on
Aircraft "1" -- "n" Seat : carries
Flight "1" -- "0..*" Ticket : has
Crew "n" -- "1" Flight : crews
Ticket "1" -- "1" Seat : reserves
User "1" -- "0..*" Ticket : buys
Ticket "1" -- "1" Email : generates
User "1" -- "0..*" Payment : pays
Payment "1" -- "1" Email : generates

@enduml